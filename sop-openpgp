#!/usr/bin/env node
const yargs = require('yargs');

const encrypt = require('./encrypt');

yargs
  .command('encrypt [certfile]', 'Encrypt...', (yargs) => {}, async (argv) => {
    encrypt(argv.certfile);
  })
  .help()
  .alias('help', 'h')
  .argv;


